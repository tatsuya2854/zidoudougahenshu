# 08. マルチエージェント運用（Claude Code subagents）

## 何を変えたか
アプリ本体（`backend/`, `frontend/`）は **一切変えていない**。追加したのは Claude Code の運用設定だけ。

```
CLAUDE.md                 司令塔（メイン agent）の指示書: プロジェクト構成・コマンド・不変条件・委譲ルール
.claude/agents/
  analyst.md              動画・編集傾向分析（読み取り専用, sonnet）
  editor.md               編集処理の実装・変更（書き込み可, メインと同モデル）
  qa.md                   品質チェック（テスト/出力検証/不変条件監査, sonnet）
  improver.md             改善: 人間修正・所見 → プロンプト/DNA/ルール更新（書き込み可, メインと同モデル）
docs/08_…                 このドキュメント
```

## 有効化の注意
`.claude/agents/*.md` は **Claude Code のセッション開始時に読み込まれる**。追加・変更した直後の同一セッションでは `subagent_type: editor` 等が見つからない（実際、導入テストでは `general-purpose` に指示書を読ませて代用した）。新しいセッションを開けばそのまま使える。

## 運用モデル
- ユーザーはメイン agent（Claude Code のセッション）にだけ話す。agent ごとの指示は出さない
- メイン agent = 司令塔。タスクを分解し、**独立していて並列化できるものだけ** を subagent に委譲。逐次依存・単純作業・1〜2 ファイルの修正は自分でやる
- 4 役は基本形であって固定ではない。仕事の無い役は起動しない。役に無い専門性は `general-purpose` に指示書を渡す
- 並列 agent は書き込み範囲を分けて衝突を防ぐ（editor 2 体なら別ディレクトリ）
- 編集系 agent の報告は qa（または司令塔自身の pytest）で裏取りしてから採用

### 委譲判断の表
| 状況 | 誰がやる |
|---|---|
| 現状把握（ファイルを数個読む） | 司令塔 |
| 候補の質・傾向・原価の評価（数字が要る） | analyst |
| パイプライン / 画面の機能追加（互いに独立な複数件） | editor × N（並列、範囲分割） |
| 1 ファイルのバグ修正 | 司令塔 |
| 変更後の合否判定、出力 MP4 の検証 | qa |
| 人間の修正が溜まった → プロンプト / ルール更新 | improver |
| commit / push / ユーザーへの報告 | 司令塔 |

## 導入テスト（2026-09-18 実施）
テストシナリオ: 「字幕アニメ `typewriter` が `CaptionStyle` の選択肢にあるのに未実装」という実タスクで、司令塔が分解して委譲。

| 順序 | agent | 担当 | 結果 |
|---|---|---|---|
| 並列① | analyst | 現在の候補選定プロンプトと Phase1 出力を読み、Phase 2 に向けた評価軸の不足を数字で指摘 | 下記「結果ログ」参照 |
| 並列② | editor | `providers/caption/ass_caption.py` に typewriter を実装し、合成動画で検証 | 〃 |
| 逐次③ | qa | editor 完了後に pytest + 出力 MP4 + 不変条件監査 | 〃 |
| 未起動 | improver | 人間の修正ログ（HumanEdit）がまだ無いので起動しない（「必要な agent だけ」の実証） | – |

### 結果ログ（2026-09-18）
| agent | 結果 |
|---|---|
| editor | `ass_caption.py` に `_typewriter`（各文字を `\alpha` + `\t` で順に表示、`\N` 保持）。pytest PASS、1.3s/2.0s/2.6s のフレームで段階表示を目視確認。`\k` 系を避けた理由（縁取りが先に見える）も報告 |
| analyst | 候補選定が Creator DNA の構造化フィールドを 10 項目中 1 項目（辞書）しか使っていない／書き出し経路が採用判定を記録せず HumanEdit が 0 件／プロンプトの固定値（20〜60 秒・余韻 1 秒）は EditingDNA から注入すべき／pricing.yaml と docs/06 は数式一致（差分 3%・10% の 2 箇所） |
| qa | **PASS**。pytest 1 passed、diff は 1 ファイル 25 行のみ、秘密情報なし、1080×1920＋音声、typewriter 動作目視、none/pop/fade 回帰 OK。既存負債として `services/reframe.py` の `import cv2`（不変条件 2 違反）を申し送り |
| improver | 未起動（HumanEdit が 0 件で学習材料が無い） |
| 司令塔 | qa/analyst の所見のうち小さいものを自分で修正: ①書き出し時に採用判定を HumanEdit へ記録（`api/exports.py`）②フレーム取得を `VideoProcessingProvider.sample_frames` に移し `reframe.py` から cv2 依存を除去。pytest PASS を確認して commit |

### 結果ログ（2026-09-24: Codex 版 `creator-studio` の強みを移植して上位互換化）
| agent | 担当 | 結果 |
|---|---|---|
| editor-1 | ローカル文字起こし既定化（auto）/ SRT 読込 / 文字起こし再利用の再解析 | 6 テスト追加。HuggingFace がこの環境から遮断のため実モデル完走は未検証（Mac で要確認） |
| editor-2 | 候補の手直しパネル（開始/終了・タイトル・構図・字幕位置/サイズ/本文）+ 全部 HumanEdit に before/after 記録 + 旧 DB 自動マイグレーション | 2 テスト追加。ついでに「center 指定でも顔追従が走る」既存バグを修正 |
| editor-3 | ZIP 一括DL（MP4+SRT+EditPlan+README+原価）/ SRT 書き出し / Mac .app ランチャー / start.* の統合 | 2 テスト追加。日本語ファイル名の Content-Disposition を RFC 5987 で解決 |
| qa | pytest 11 / tsc / build / 不変条件監査 / center・manual クロップの実証 / 実ブラウザ E2E（スマホ幅含む）/ ZIP 中身 / ランチャー | **PASS(条件付き)**。高: 書き出し済み動画の再解析が外部キーで落ちて「解析中」で固まる（既存バグ、SRT 導線で必ず踏む）。低: ZIP の README が手直し前の区間を表示 |
| 司令塔 | 統合修正: 手直し後タイトルを書き出し名/一覧に反映、`.gitignore` に `*.app/`、qa の高/低を修正（superseded 化 + 失敗時 status 復帰 + 回帰テスト）、README に GUI 手順 | pytest 12 passed で commit |
| analyst / improver | 未起動（分析材料・修正ログがまだ無い） | – |

### 持ち越し（次回の委譲候補）
- improver: `build_system_prompt` に EditingDNA の `shorts_policy.target_duration` / `pacing` を注入するプレースホルダ、CreatorDNA の構造化サブフィールドの直接注入、docs/06 の内訳明記
- editor: heuristic 経路の `scores{}` 全項目 50 固定の改善、`_wrap` の末尾句読点の折返し

